{
    // See https://go.microsoft.com/fwlink/?LinkId=733558
    // for the documentation about the tasks.json format
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Start mongodb",
            "type": "shell",
            "command": "docker-compose -f .devcontainer/backend.yml up -d mongo"
        },
        {
            "label": "Start mongodb-express",
            "type": "shell",
            "command": "docker-compose -f .devcontainer/backend.yml up -d mongo-express"
        }
    ]
}